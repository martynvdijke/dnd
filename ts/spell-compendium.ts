/**
 * Spell Compendium initialization.
 *
 * Handles the spell compendium browse: ensures HTMX-based
 * filter/search behavior is properly initialized.
 */
export function initSpellCompendium(): void {
  // HTMX-driven - all behavior is declarative in templates via hx-get/hx-trigger
  // No manual JS wiring needed beyond module registration.
}

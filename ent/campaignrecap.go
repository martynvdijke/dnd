// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/campaign"
	"villum/ent/campaignrecap"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CampaignRecap is the model entity for the CampaignRecap schema.
type CampaignRecap struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CampaignID holds the value of the "campaign_id" field.
	CampaignID int64 `json:"campaign_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Content holds the value of the "content" field.
	Content string `json:"content,omitempty"`
	// SessionStartDate holds the value of the "session_start_date" field.
	SessionStartDate string `json:"session_start_date,omitempty"`
	// SessionEndDate holds the value of the "session_end_date" field.
	SessionEndDate string `json:"session_end_date,omitempty"`
	// WordCount holds the value of the "word_count" field.
	WordCount int `json:"word_count,omitempty"`
	// IsEdited holds the value of the "is_edited" field.
	IsEdited bool `json:"is_edited,omitempty"`
	// IsSent holds the value of the "is_sent" field.
	IsSent bool `json:"is_sent,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt string `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CampaignRecapQuery when eager-loading is set.
	Edges        CampaignRecapEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CampaignRecapEdges holds the relations/edges for other nodes in the graph.
type CampaignRecapEdges struct {
	// Campaign holds the value of the campaign edge.
	Campaign *Campaign `json:"campaign,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CampaignOrErr returns the Campaign value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CampaignRecapEdges) CampaignOrErr() (*Campaign, error) {
	if e.Campaign != nil {
		return e.Campaign, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: campaign.Label}
	}
	return nil, &NotLoadedError{edge: "campaign"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CampaignRecap) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case campaignrecap.FieldIsEdited, campaignrecap.FieldIsSent:
			values[i] = new(sql.NullBool)
		case campaignrecap.FieldID, campaignrecap.FieldCampaignID, campaignrecap.FieldWordCount:
			values[i] = new(sql.NullInt64)
		case campaignrecap.FieldTitle, campaignrecap.FieldContent, campaignrecap.FieldSessionStartDate, campaignrecap.FieldSessionEndDate, campaignrecap.FieldCreatedAt:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CampaignRecap fields.
func (_m *CampaignRecap) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case campaignrecap.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case campaignrecap.FieldCampaignID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field campaign_id", values[i])
			} else if value.Valid {
				_m.CampaignID = value.Int64
			}
		case campaignrecap.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case campaignrecap.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				_m.Content = value.String
			}
		case campaignrecap.FieldSessionStartDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_start_date", values[i])
			} else if value.Valid {
				_m.SessionStartDate = value.String
			}
		case campaignrecap.FieldSessionEndDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_end_date", values[i])
			} else if value.Valid {
				_m.SessionEndDate = value.String
			}
		case campaignrecap.FieldWordCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field word_count", values[i])
			} else if value.Valid {
				_m.WordCount = int(value.Int64)
			}
		case campaignrecap.FieldIsEdited:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_edited", values[i])
			} else if value.Valid {
				_m.IsEdited = value.Bool
			}
		case campaignrecap.FieldIsSent:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_sent", values[i])
			} else if value.Valid {
				_m.IsSent = value.Bool
			}
		case campaignrecap.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CampaignRecap.
// This includes values selected through modifiers, order, etc.
func (_m *CampaignRecap) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCampaign queries the "campaign" edge of the CampaignRecap entity.
func (_m *CampaignRecap) QueryCampaign() *CampaignQuery {
	return NewCampaignRecapClient(_m.config).QueryCampaign(_m)
}

// Update returns a builder for updating this CampaignRecap.
// Note that you need to call CampaignRecap.Unwrap() before calling this method if this CampaignRecap
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CampaignRecap) Update() *CampaignRecapUpdateOne {
	return NewCampaignRecapClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CampaignRecap entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CampaignRecap) Unwrap() *CampaignRecap {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CampaignRecap is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CampaignRecap) String() string {
	var builder strings.Builder
	builder.WriteString("CampaignRecap(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("campaign_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CampaignID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(_m.Content)
	builder.WriteString(", ")
	builder.WriteString("session_start_date=")
	builder.WriteString(_m.SessionStartDate)
	builder.WriteString(", ")
	builder.WriteString("session_end_date=")
	builder.WriteString(_m.SessionEndDate)
	builder.WriteString(", ")
	builder.WriteString("word_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.WordCount))
	builder.WriteString(", ")
	builder.WriteString("is_edited=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsEdited))
	builder.WriteString(", ")
	builder.WriteString("is_sent=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsSent))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt)
	builder.WriteByte(')')
	return builder.String()
}

// CampaignRecaps is a parsable slice of CampaignRecap.
type CampaignRecaps []*CampaignRecap

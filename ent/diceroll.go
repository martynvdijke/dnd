// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/diceroll"
	"villum/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DiceRoll is the model entity for the DiceRoll schema.
type DiceRoll struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int64 `json:"user_id,omitempty"`
	// CharacterID holds the value of the "character_id" field.
	CharacterID int64 `json:"character_id,omitempty"`
	// Expression holds the value of the "expression" field.
	Expression string `json:"expression,omitempty"`
	// Result holds the value of the "result" field.
	Result string `json:"result,omitempty"`
	// Total holds the value of the "total" field.
	Total int `json:"total,omitempty"`
	// Timestamp holds the value of the "timestamp" field.
	Timestamp string `json:"timestamp,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DiceRollQuery when eager-loading is set.
	Edges        DiceRollEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DiceRollEdges holds the relations/edges for other nodes in the graph.
type DiceRollEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DiceRollEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DiceRoll) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case diceroll.FieldID, diceroll.FieldUserID, diceroll.FieldCharacterID, diceroll.FieldTotal:
			values[i] = new(sql.NullInt64)
		case diceroll.FieldExpression, diceroll.FieldResult, diceroll.FieldTimestamp:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DiceRoll fields.
func (_m *DiceRoll) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case diceroll.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case diceroll.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.Int64
			}
		case diceroll.FieldCharacterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field character_id", values[i])
			} else if value.Valid {
				_m.CharacterID = value.Int64
			}
		case diceroll.FieldExpression:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field expression", values[i])
			} else if value.Valid {
				_m.Expression = value.String
			}
		case diceroll.FieldResult:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field result", values[i])
			} else if value.Valid {
				_m.Result = value.String
			}
		case diceroll.FieldTotal:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total", values[i])
			} else if value.Valid {
				_m.Total = int(value.Int64)
			}
		case diceroll.FieldTimestamp:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field timestamp", values[i])
			} else if value.Valid {
				_m.Timestamp = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DiceRoll.
// This includes values selected through modifiers, order, etc.
func (_m *DiceRoll) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the DiceRoll entity.
func (_m *DiceRoll) QueryUser() *UserQuery {
	return NewDiceRollClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this DiceRoll.
// Note that you need to call DiceRoll.Unwrap() before calling this method if this DiceRoll
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DiceRoll) Update() *DiceRollUpdateOne {
	return NewDiceRollClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DiceRoll entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DiceRoll) Unwrap() *DiceRoll {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DiceRoll is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DiceRoll) String() string {
	var builder strings.Builder
	builder.WriteString("DiceRoll(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("character_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CharacterID))
	builder.WriteString(", ")
	builder.WriteString("expression=")
	builder.WriteString(_m.Expression)
	builder.WriteString(", ")
	builder.WriteString("result=")
	builder.WriteString(_m.Result)
	builder.WriteString(", ")
	builder.WriteString("total=")
	builder.WriteString(fmt.Sprintf("%v", _m.Total))
	builder.WriteString(", ")
	builder.WriteString("timestamp=")
	builder.WriteString(_m.Timestamp)
	builder.WriteByte(')')
	return builder.String()
}

// DiceRolls is a parsable slice of DiceRoll.
type DiceRolls []*DiceRoll

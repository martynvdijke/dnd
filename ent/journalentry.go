// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/character"
	"villum/ent/journalentry"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// JournalEntry is the model entity for the JournalEntry schema.
type JournalEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CharacterID holds the value of the "character_id" field.
	CharacterID int64 `json:"character_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Entry holds the value of the "entry" field.
	Entry string `json:"entry,omitempty"`
	// EntryDate holds the value of the "entry_date" field.
	EntryDate string `json:"entry_date,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt string `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JournalEntryQuery when eager-loading is set.
	Edges        JournalEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// JournalEntryEdges holds the relations/edges for other nodes in the graph.
type JournalEntryEdges struct {
	// Character holds the value of the character edge.
	Character *Character `json:"character,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CharacterOrErr returns the Character value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JournalEntryEdges) CharacterOrErr() (*Character, error) {
	if e.Character != nil {
		return e.Character, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: character.Label}
	}
	return nil, &NotLoadedError{edge: "character"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JournalEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case journalentry.FieldID, journalentry.FieldCharacterID:
			values[i] = new(sql.NullInt64)
		case journalentry.FieldTitle, journalentry.FieldEntry, journalentry.FieldEntryDate, journalentry.FieldCreatedAt:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JournalEntry fields.
func (_m *JournalEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case journalentry.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case journalentry.FieldCharacterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field character_id", values[i])
			} else if value.Valid {
				_m.CharacterID = value.Int64
			}
		case journalentry.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case journalentry.FieldEntry:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entry", values[i])
			} else if value.Valid {
				_m.Entry = value.String
			}
		case journalentry.FieldEntryDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entry_date", values[i])
			} else if value.Valid {
				_m.EntryDate = value.String
			}
		case journalentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JournalEntry.
// This includes values selected through modifiers, order, etc.
func (_m *JournalEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCharacter queries the "character" edge of the JournalEntry entity.
func (_m *JournalEntry) QueryCharacter() *CharacterQuery {
	return NewJournalEntryClient(_m.config).QueryCharacter(_m)
}

// Update returns a builder for updating this JournalEntry.
// Note that you need to call JournalEntry.Unwrap() before calling this method if this JournalEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *JournalEntry) Update() *JournalEntryUpdateOne {
	return NewJournalEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the JournalEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *JournalEntry) Unwrap() *JournalEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: JournalEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *JournalEntry) String() string {
	var builder strings.Builder
	builder.WriteString("JournalEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("character_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CharacterID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("entry=")
	builder.WriteString(_m.Entry)
	builder.WriteString(", ")
	builder.WriteString("entry_date=")
	builder.WriteString(_m.EntryDate)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt)
	builder.WriteByte(')')
	return builder.String()
}

// JournalEntries is a parsable slice of JournalEntry.
type JournalEntries []*JournalEntry

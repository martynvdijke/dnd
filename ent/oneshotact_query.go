// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"villum/ent/oneshotact"
	"villum/ent/oneshotadventure"
	"villum/ent/oneshotadventureencounter"
	"villum/ent/oneshotitem"
	"villum/ent/oneshotscene"
	"villum/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// OneShotActQuery is the builder for querying OneShotAct entities.
type OneShotActQuery struct {
	config
	ctx            *QueryContext
	order          []oneshotact.OrderOption
	inters         []Interceptor
	predicates     []predicate.OneShotAct
	withAdventure  *OneShotAdventureQuery
	withScenes     *OneShotSceneQuery
	withChildren   *OneShotActQuery
	withParent     *OneShotActQuery
	withItems      *OneShotItemQuery
	withEncounters *OneShotAdventureEncounterQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OneShotActQuery builder.
func (_q *OneShotActQuery) Where(ps ...predicate.OneShotAct) *OneShotActQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *OneShotActQuery) Limit(limit int) *OneShotActQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *OneShotActQuery) Offset(offset int) *OneShotActQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *OneShotActQuery) Unique(unique bool) *OneShotActQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *OneShotActQuery) Order(o ...oneshotact.OrderOption) *OneShotActQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAdventure chains the current query on the "adventure" edge.
func (_q *OneShotActQuery) QueryAdventure() *OneShotAdventureQuery {
	query := (&OneShotAdventureClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotadventure.Table, oneshotadventure.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oneshotact.AdventureTable, oneshotact.AdventureColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryScenes chains the current query on the "scenes" edge.
func (_q *OneShotActQuery) QueryScenes() *OneShotSceneQuery {
	query := (&OneShotSceneClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotscene.Table, oneshotscene.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oneshotact.ScenesTable, oneshotact.ScenesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildren chains the current query on the "children" edge.
func (_q *OneShotActQuery) QueryChildren() *OneShotActQuery {
	query := (&OneShotActClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotact.Table, oneshotact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oneshotact.ChildrenTable, oneshotact.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParent chains the current query on the "parent" edge.
func (_q *OneShotActQuery) QueryParent() *OneShotActQuery {
	query := (&OneShotActClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotact.Table, oneshotact.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oneshotact.ParentTable, oneshotact.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryItems chains the current query on the "items" edge.
func (_q *OneShotActQuery) QueryItems() *OneShotItemQuery {
	query := (&OneShotItemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotitem.Table, oneshotitem.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oneshotact.ItemsTable, oneshotact.ItemsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEncounters chains the current query on the "encounters" edge.
func (_q *OneShotActQuery) QueryEncounters() *OneShotAdventureEncounterQuery {
	query := (&OneShotAdventureEncounterClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oneshotact.Table, oneshotact.FieldID, selector),
			sqlgraph.To(oneshotadventureencounter.Table, oneshotadventureencounter.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oneshotact.EncountersTable, oneshotact.EncountersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OneShotAct entity from the query.
// Returns a *NotFoundError when no OneShotAct was found.
func (_q *OneShotActQuery) First(ctx context.Context) (*OneShotAct, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oneshotact.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *OneShotActQuery) FirstX(ctx context.Context) *OneShotAct {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OneShotAct ID from the query.
// Returns a *NotFoundError when no OneShotAct ID was found.
func (_q *OneShotActQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oneshotact.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *OneShotActQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OneShotAct entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OneShotAct entity is found.
// Returns a *NotFoundError when no OneShotAct entities are found.
func (_q *OneShotActQuery) Only(ctx context.Context) (*OneShotAct, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oneshotact.Label}
	default:
		return nil, &NotSingularError{oneshotact.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *OneShotActQuery) OnlyX(ctx context.Context) *OneShotAct {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OneShotAct ID in the query.
// Returns a *NotSingularError when more than one OneShotAct ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *OneShotActQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oneshotact.Label}
	default:
		err = &NotSingularError{oneshotact.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *OneShotActQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OneShotActs.
func (_q *OneShotActQuery) All(ctx context.Context) ([]*OneShotAct, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OneShotAct, *OneShotActQuery]()
	return withInterceptors[[]*OneShotAct](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *OneShotActQuery) AllX(ctx context.Context) []*OneShotAct {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OneShotAct IDs.
func (_q *OneShotActQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(oneshotact.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *OneShotActQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *OneShotActQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*OneShotActQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *OneShotActQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *OneShotActQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *OneShotActQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OneShotActQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *OneShotActQuery) Clone() *OneShotActQuery {
	if _q == nil {
		return nil
	}
	return &OneShotActQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]oneshotact.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.OneShotAct{}, _q.predicates...),
		withAdventure:  _q.withAdventure.Clone(),
		withScenes:     _q.withScenes.Clone(),
		withChildren:   _q.withChildren.Clone(),
		withParent:     _q.withParent.Clone(),
		withItems:      _q.withItems.Clone(),
		withEncounters: _q.withEncounters.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAdventure tells the query-builder to eager-load the nodes that are connected to
// the "adventure" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithAdventure(opts ...func(*OneShotAdventureQuery)) *OneShotActQuery {
	query := (&OneShotAdventureClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAdventure = query
	return _q
}

// WithScenes tells the query-builder to eager-load the nodes that are connected to
// the "scenes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithScenes(opts ...func(*OneShotSceneQuery)) *OneShotActQuery {
	query := (&OneShotSceneClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withScenes = query
	return _q
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithChildren(opts ...func(*OneShotActQuery)) *OneShotActQuery {
	query := (&OneShotActClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChildren = query
	return _q
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithParent(opts ...func(*OneShotActQuery)) *OneShotActQuery {
	query := (&OneShotActClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParent = query
	return _q
}

// WithItems tells the query-builder to eager-load the nodes that are connected to
// the "items" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithItems(opts ...func(*OneShotItemQuery)) *OneShotActQuery {
	query := (&OneShotItemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withItems = query
	return _q
}

// WithEncounters tells the query-builder to eager-load the nodes that are connected to
// the "encounters" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OneShotActQuery) WithEncounters(opts ...func(*OneShotAdventureEncounterQuery)) *OneShotActQuery {
	query := (&OneShotAdventureEncounterClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEncounters = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AdventureID int64 `json:"adventure_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OneShotAct.Query().
//		GroupBy(oneshotact.FieldAdventureID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *OneShotActQuery) GroupBy(field string, fields ...string) *OneShotActGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OneShotActGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = oneshotact.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AdventureID int64 `json:"adventure_id,omitempty"`
//	}
//
//	client.OneShotAct.Query().
//		Select(oneshotact.FieldAdventureID).
//		Scan(ctx, &v)
func (_q *OneShotActQuery) Select(fields ...string) *OneShotActSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &OneShotActSelect{OneShotActQuery: _q}
	sbuild.label = oneshotact.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OneShotActSelect configured with the given aggregations.
func (_q *OneShotActQuery) Aggregate(fns ...AggregateFunc) *OneShotActSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *OneShotActQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !oneshotact.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *OneShotActQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OneShotAct, error) {
	var (
		nodes       = []*OneShotAct{}
		_spec       = _q.querySpec()
		loadedTypes = [6]bool{
			_q.withAdventure != nil,
			_q.withScenes != nil,
			_q.withChildren != nil,
			_q.withParent != nil,
			_q.withItems != nil,
			_q.withEncounters != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OneShotAct).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OneShotAct{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAdventure; query != nil {
		if err := _q.loadAdventure(ctx, query, nodes, nil,
			func(n *OneShotAct, e *OneShotAdventure) { n.Edges.Adventure = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withScenes; query != nil {
		if err := _q.loadScenes(ctx, query, nodes,
			func(n *OneShotAct) { n.Edges.Scenes = []*OneShotScene{} },
			func(n *OneShotAct, e *OneShotScene) { n.Edges.Scenes = append(n.Edges.Scenes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChildren; query != nil {
		if err := _q.loadChildren(ctx, query, nodes,
			func(n *OneShotAct) { n.Edges.Children = []*OneShotAct{} },
			func(n *OneShotAct, e *OneShotAct) { n.Edges.Children = append(n.Edges.Children, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParent; query != nil {
		if err := _q.loadParent(ctx, query, nodes, nil,
			func(n *OneShotAct, e *OneShotAct) { n.Edges.Parent = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withItems; query != nil {
		if err := _q.loadItems(ctx, query, nodes,
			func(n *OneShotAct) { n.Edges.Items = []*OneShotItem{} },
			func(n *OneShotAct, e *OneShotItem) { n.Edges.Items = append(n.Edges.Items, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEncounters; query != nil {
		if err := _q.loadEncounters(ctx, query, nodes,
			func(n *OneShotAct) { n.Edges.Encounters = []*OneShotAdventureEncounter{} },
			func(n *OneShotAct, e *OneShotAdventureEncounter) { n.Edges.Encounters = append(n.Edges.Encounters, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *OneShotActQuery) loadAdventure(ctx context.Context, query *OneShotAdventureQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotAdventure)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*OneShotAct)
	for i := range nodes {
		fk := nodes[i].AdventureID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(oneshotadventure.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "adventure_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *OneShotActQuery) loadScenes(ctx context.Context, query *OneShotSceneQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotScene)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*OneShotAct)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oneshotscene.FieldActID)
	}
	query.Where(predicate.OneShotScene(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oneshotact.ScenesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ActID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OneShotActQuery) loadChildren(ctx context.Context, query *OneShotActQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotAct)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*OneShotAct)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oneshotact.FieldParentActID)
	}
	query.Where(predicate.OneShotAct(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oneshotact.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentActID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_act_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OneShotActQuery) loadParent(ctx context.Context, query *OneShotActQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotAct)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*OneShotAct)
	for i := range nodes {
		fk := nodes[i].ParentActID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(oneshotact.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_act_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *OneShotActQuery) loadItems(ctx context.Context, query *OneShotItemQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotItem)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*OneShotAct)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oneshotitem.FieldActID)
	}
	query.Where(predicate.OneShotItem(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oneshotact.ItemsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ActID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OneShotActQuery) loadEncounters(ctx context.Context, query *OneShotAdventureEncounterQuery, nodes []*OneShotAct, init func(*OneShotAct), assign func(*OneShotAct, *OneShotAdventureEncounter)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*OneShotAct)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oneshotadventureencounter.FieldActID)
	}
	query.Where(predicate.OneShotAdventureEncounter(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oneshotact.EncountersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ActID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *OneShotActQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *OneShotActQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oneshotact.Table, oneshotact.Columns, sqlgraph.NewFieldSpec(oneshotact.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oneshotact.FieldID)
		for i := range fields {
			if fields[i] != oneshotact.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAdventure != nil {
			_spec.Node.AddColumnOnce(oneshotact.FieldAdventureID)
		}
		if _q.withParent != nil {
			_spec.Node.AddColumnOnce(oneshotact.FieldParentActID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *OneShotActQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(oneshotact.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = oneshotact.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OneShotActGroupBy is the group-by builder for OneShotAct entities.
type OneShotActGroupBy struct {
	selector
	build *OneShotActQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *OneShotActGroupBy) Aggregate(fns ...AggregateFunc) *OneShotActGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *OneShotActGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OneShotActQuery, *OneShotActGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *OneShotActGroupBy) sqlScan(ctx context.Context, root *OneShotActQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OneShotActSelect is the builder for selecting fields of OneShotAct entities.
type OneShotActSelect struct {
	*OneShotActQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *OneShotActSelect) Aggregate(fns ...AggregateFunc) *OneShotActSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *OneShotActSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OneShotActQuery, *OneShotActSelect](ctx, _s.OneShotActQuery, _s, _s.inters, v)
}

func (_s *OneShotActSelect) sqlScan(ctx context.Context, root *OneShotActQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

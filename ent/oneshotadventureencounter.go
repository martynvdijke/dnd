// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/oneshotact"
	"villum/ent/oneshotadventureencounter"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// OneShotAdventureEncounter is the model entity for the OneShotAdventureEncounter schema.
type OneShotAdventureEncounter struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// AdventureID holds the value of the "adventure_id" field.
	AdventureID int64 `json:"adventure_id,omitempty"`
	// ActID holds the value of the "act_id" field.
	ActID int64 `json:"act_id,omitempty"`
	// EncounterID holds the value of the "encounter_id" field.
	EncounterID int64 `json:"encounter_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OneShotAdventureEncounterQuery when eager-loading is set.
	Edges        OneShotAdventureEncounterEdges `json:"edges"`
	selectValues sql.SelectValues
}

// OneShotAdventureEncounterEdges holds the relations/edges for other nodes in the graph.
type OneShotAdventureEncounterEdges struct {
	// Act holds the value of the act edge.
	Act *OneShotAct `json:"act,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ActOrErr returns the Act value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OneShotAdventureEncounterEdges) ActOrErr() (*OneShotAct, error) {
	if e.Act != nil {
		return e.Act, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: oneshotact.Label}
	}
	return nil, &NotLoadedError{edge: "act"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OneShotAdventureEncounter) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case oneshotadventureencounter.FieldID, oneshotadventureencounter.FieldAdventureID, oneshotadventureencounter.FieldActID, oneshotadventureencounter.FieldEncounterID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OneShotAdventureEncounter fields.
func (_m *OneShotAdventureEncounter) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case oneshotadventureencounter.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case oneshotadventureencounter.FieldAdventureID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field adventure_id", values[i])
			} else if value.Valid {
				_m.AdventureID = value.Int64
			}
		case oneshotadventureencounter.FieldActID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field act_id", values[i])
			} else if value.Valid {
				_m.ActID = value.Int64
			}
		case oneshotadventureencounter.FieldEncounterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field encounter_id", values[i])
			} else if value.Valid {
				_m.EncounterID = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OneShotAdventureEncounter.
// This includes values selected through modifiers, order, etc.
func (_m *OneShotAdventureEncounter) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAct queries the "act" edge of the OneShotAdventureEncounter entity.
func (_m *OneShotAdventureEncounter) QueryAct() *OneShotActQuery {
	return NewOneShotAdventureEncounterClient(_m.config).QueryAct(_m)
}

// Update returns a builder for updating this OneShotAdventureEncounter.
// Note that you need to call OneShotAdventureEncounter.Unwrap() before calling this method if this OneShotAdventureEncounter
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *OneShotAdventureEncounter) Update() *OneShotAdventureEncounterUpdateOne {
	return NewOneShotAdventureEncounterClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the OneShotAdventureEncounter entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *OneShotAdventureEncounter) Unwrap() *OneShotAdventureEncounter {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: OneShotAdventureEncounter is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *OneShotAdventureEncounter) String() string {
	var builder strings.Builder
	builder.WriteString("OneShotAdventureEncounter(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("adventure_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AdventureID))
	builder.WriteString(", ")
	builder.WriteString("act_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ActID))
	builder.WriteString(", ")
	builder.WriteString("encounter_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EncounterID))
	builder.WriteByte(')')
	return builder.String()
}

// OneShotAdventureEncounters is a parsable slice of OneShotAdventureEncounter.
type OneShotAdventureEncounters []*OneShotAdventureEncounter

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/shop"
	"villum/ent/shopitem"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ShopItem is the model entity for the ShopItem schema.
type ShopItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// ShopID holds the value of the "shop_id" field.
	ShopID int64 `json:"shop_id,omitempty"`
	// ItemName holds the value of the "item_name" field.
	ItemName string `json:"item_name,omitempty"`
	// Category holds the value of the "category" field.
	Category string `json:"category,omitempty"`
	// PriceGp holds the value of the "price_gp" field.
	PriceGp float64 `json:"price_gp,omitempty"`
	// QuantityAvailable holds the value of the "quantity_available" field.
	QuantityAvailable int `json:"quantity_available,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// IsMagical holds the value of the "is_magical" field.
	IsMagical bool `json:"is_magical,omitempty"`
	// AttunementRequired holds the value of the "attunement_required" field.
	AttunementRequired bool `json:"attunement_required,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShopItemQuery when eager-loading is set.
	Edges        ShopItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShopItemEdges holds the relations/edges for other nodes in the graph.
type ShopItemEdges struct {
	// Shop holds the value of the shop edge.
	Shop *Shop `json:"shop,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ShopOrErr returns the Shop value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShopItemEdges) ShopOrErr() (*Shop, error) {
	if e.Shop != nil {
		return e.Shop, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: shop.Label}
	}
	return nil, &NotLoadedError{edge: "shop"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShopItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shopitem.FieldIsMagical, shopitem.FieldAttunementRequired:
			values[i] = new(sql.NullBool)
		case shopitem.FieldPriceGp:
			values[i] = new(sql.NullFloat64)
		case shopitem.FieldID, shopitem.FieldShopID, shopitem.FieldQuantityAvailable:
			values[i] = new(sql.NullInt64)
		case shopitem.FieldItemName, shopitem.FieldCategory, shopitem.FieldDescription, shopitem.FieldNotes:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShopItem fields.
func (_m *ShopItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shopitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case shopitem.FieldShopID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field shop_id", values[i])
			} else if value.Valid {
				_m.ShopID = value.Int64
			}
		case shopitem.FieldItemName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field item_name", values[i])
			} else if value.Valid {
				_m.ItemName = value.String
			}
		case shopitem.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				_m.Category = value.String
			}
		case shopitem.FieldPriceGp:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price_gp", values[i])
			} else if value.Valid {
				_m.PriceGp = value.Float64
			}
		case shopitem.FieldQuantityAvailable:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity_available", values[i])
			} else if value.Valid {
				_m.QuantityAvailable = int(value.Int64)
			}
		case shopitem.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case shopitem.FieldIsMagical:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_magical", values[i])
			} else if value.Valid {
				_m.IsMagical = value.Bool
			}
		case shopitem.FieldAttunementRequired:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field attunement_required", values[i])
			} else if value.Valid {
				_m.AttunementRequired = value.Bool
			}
		case shopitem.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShopItem.
// This includes values selected through modifiers, order, etc.
func (_m *ShopItem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryShop queries the "shop" edge of the ShopItem entity.
func (_m *ShopItem) QueryShop() *ShopQuery {
	return NewShopItemClient(_m.config).QueryShop(_m)
}

// Update returns a builder for updating this ShopItem.
// Note that you need to call ShopItem.Unwrap() before calling this method if this ShopItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ShopItem) Update() *ShopItemUpdateOne {
	return NewShopItemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ShopItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ShopItem) Unwrap() *ShopItem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShopItem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ShopItem) String() string {
	var builder strings.Builder
	builder.WriteString("ShopItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("shop_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ShopID))
	builder.WriteString(", ")
	builder.WriteString("item_name=")
	builder.WriteString(_m.ItemName)
	builder.WriteString(", ")
	builder.WriteString("category=")
	builder.WriteString(_m.Category)
	builder.WriteString(", ")
	builder.WriteString("price_gp=")
	builder.WriteString(fmt.Sprintf("%v", _m.PriceGp))
	builder.WriteString(", ")
	builder.WriteString("quantity_available=")
	builder.WriteString(fmt.Sprintf("%v", _m.QuantityAvailable))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("is_magical=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsMagical))
	builder.WriteString(", ")
	builder.WriteString("attunement_required=")
	builder.WriteString(fmt.Sprintf("%v", _m.AttunementRequired))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteByte(')')
	return builder.String()
}

// ShopItems is a parsable slice of ShopItem.
type ShopItems []*ShopItem

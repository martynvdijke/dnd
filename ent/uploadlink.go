// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"villum/ent/uploadlink"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UploadLink is the model entity for the UploadLink schema.
type UploadLink struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// UploadID holds the value of the "upload_id" field.
	UploadID int64 `json:"upload_id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType string `json:"entity_type,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID int64 `json:"entity_id,omitempty"`
	// FieldName holds the value of the "field_name" field.
	FieldName string `json:"field_name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt           string `json:"created_at,omitempty"`
	upload_upload_links *int64
	selectValues        sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UploadLink) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case uploadlink.FieldID, uploadlink.FieldUploadID, uploadlink.FieldEntityID:
			values[i] = new(sql.NullInt64)
		case uploadlink.FieldEntityType, uploadlink.FieldFieldName, uploadlink.FieldCreatedAt:
			values[i] = new(sql.NullString)
		case uploadlink.ForeignKeys[0]: // upload_upload_links
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UploadLink fields.
func (_m *UploadLink) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case uploadlink.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case uploadlink.FieldUploadID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field upload_id", values[i])
			} else if value.Valid {
				_m.UploadID = value.Int64
			}
		case uploadlink.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				_m.EntityType = value.String
			}
		case uploadlink.FieldEntityID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				_m.EntityID = value.Int64
			}
		case uploadlink.FieldFieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field field_name", values[i])
			} else if value.Valid {
				_m.FieldName = value.String
			}
		case uploadlink.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.String
			}
		case uploadlink.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field upload_upload_links", value)
			} else if value.Valid {
				_m.upload_upload_links = new(int64)
				*_m.upload_upload_links = int64(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UploadLink.
// This includes values selected through modifiers, order, etc.
func (_m *UploadLink) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this UploadLink.
// Note that you need to call UploadLink.Unwrap() before calling this method if this UploadLink
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UploadLink) Update() *UploadLinkUpdateOne {
	return NewUploadLinkClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UploadLink entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UploadLink) Unwrap() *UploadLink {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UploadLink is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UploadLink) String() string {
	var builder strings.Builder
	builder.WriteString("UploadLink(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("upload_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UploadID))
	builder.WriteString(", ")
	builder.WriteString("entity_type=")
	builder.WriteString(_m.EntityType)
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EntityID))
	builder.WriteString(", ")
	builder.WriteString("field_name=")
	builder.WriteString(_m.FieldName)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt)
	builder.WriteByte(')')
	return builder.String()
}

// UploadLinks is a parsable slice of UploadLink.
type UploadLinks []*UploadLink
